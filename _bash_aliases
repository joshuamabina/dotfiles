#Functions

#
# Create and change the current working directory.
#
# @param    /path/to/directory
#
mkcdir() {
    if [ ! $1 ]; then

        echo "Whoops! Try mkcdir </path/to/directory>"

    else

        mkdir -p -- "$1" && cd "$1"

    fi
}

#
# Git reset current HEAD to previous state.
#
# @param    The number of commits.
#
stfu() {
    STFU_CMD="git reset HEAD"

    if [ $1 ]; then

        STFU_CMD="${STFU_CMD}~$1"

    fi

    ${STFU_CMD}
}

#Reload
alias so='source $HOME/.bashrc'

#Globals
alias ..='cd ..'
alias ...='cd ../..'
alias ....='cd ../../..'

alias less='less -r'

alias ef='editor'
alias ed='editor .'

alias grep='grep --color=auto'
alias fgrep='fgrep --color=auto'
alias egrep='egrep --color=auto'

alias dir='dir --color=auto'
alias vdir='vdir --color=auto'

#Aptitude
alias apt-install='sudo apt-get install'
alias apt-remove='sudo apt-get remove'
alias apt-purge='sudo apt-get purge'
alias apt-autoclean='sudo apt-get autoclean'
alias apt-autoremove='sudo apt-get autoremove'

#Directories
alias la='ls -ali'
alias ll='ls -l'
alias ls='ls -Fsh -t --group-directories-first'

#Git
alias g='git'

alias ca='git commit --amend'
alias cc='git commit'
alias ga='git add'
alias gaa='git add .'
alias gap='git add -p'
alias gash='git stash'
alias gb='git branch'
alias gba='git branch -a'
alias gd='git diff'
alias gds='git diff --staged'
alias gl='git log'
alias glo='git log --oneline'
alias glone='git clone'
alias gout='git checkout'
alias gs='git status'

alias fetch='git fetch'
alias pull='git pull'
alias push='git push'

alias fu='git reset --hard && git clean -df'

#YouTube-dl
alias yt='/usr/local/bin/youtube-dl -q'

#JavaScript
alias dev='yarn dev'
alias watch='yarn watch'

#Composer
alias dump-autoload='composer dump-autoload'
alias require='composer require'
alias require-dev='composer require --dev'

#Laravel
alias art='php artisan'

alias seed='art db:seed'
alias serve='art serve'
alias tinker='art tinker'

alias migrate='art migrate && seed'
alias fresh='art migrate:fresh && seed'

alias art-clear='art cache:clear && art config:clear && art view:clear'
alias art-cache='art config:cache && art route:cache'

alias dump='dump-autoload && art-clear && art optimize'

alias opt='art-dump && art-cache'

#PHPUnit
alias unit='phpunit'

# vim: ft=conf
